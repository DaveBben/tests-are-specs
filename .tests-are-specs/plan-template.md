<!--
  tests-are-specs plan / intent template.
  Customize this freely. `scoping` fills it in as the short intent for a change,
  written to {plan_dir}/<slug>/plan.md, and `building-from-plans` and `verifying-work` read it back from there.
  The requirements live in the test stubs, not here; keep this short.
-->

## Goal
<!-- The checkable delta: "X moves from A to B", confirmable true or false. -->

## Verification
<!-- The failing test stubs that bind this change. For each stub: its test name, the
     concrete expected outcome its description pins, and where it lives. Note the command
     that runs them. These stubs ARE the requirements; do not restate them as prose.
     For anything that resists a stub (a load or latency NFR, UX feel, a security read),
     add a manual script or benchmark here and name who runs it. -->

## Out of scope
<!-- What this change deliberately does not do. -->
