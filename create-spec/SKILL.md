---
name: create-spec
description: >
  Deprecated — use `capture` instead. Retained for backwards compatibility.
  When the user invokes create-spec, redirect to the capture skill.
disable-model-invocation: true
---

# create-spec (Deprecated)

This skill has been renamed to `capture`. Please use `/capture` instead.

The `capture` skill provides the same workflow with an updated section structure
focused on representing current project state as a living document.
